import gf2

gf2_n_poly_prod(p1, p2, f):
  """Assumes that p1 and p2 are polynomials
  whose coefficients are bitvectors of length n,
  viewed as elements of GF(2^n), and that f is the
  corresponding modulus for GF(2^n), returns the
  product of p1 and p2."""
  pass
